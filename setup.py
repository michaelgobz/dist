from setuptools import setup

setup(name='prob-distributions',
      version='1.0.0',
      description='Provides functionality to work with Probability distributions and different sampling techniques',
      packages=['distributions','sampling-methods'],
      zip_safe=False)